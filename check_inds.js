const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const inds = await prisma.industry.findMany(); console.log(inds); } main().catch(console.error).finally(() => prisma.$disconnect());
